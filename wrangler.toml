name = 'me2christ-worker'
main = 'contact.js'

routes = [{ pattern = "worker.me2christ.com", custom_domain = true }]

compatibility_date = '2024-10-11'
compatibility_flags = [ 'nodejs_compat' ]

[observability]
enabled = true

[[send_email]]
name = 'SEND_EMAIL'

[vars]
DISABLE_WORKER = false

# The rate limiting API is in open beta.
[[unsafe.bindings]]
name = 'RATE_LIMITER'
type = 'ratelimit'
namespace_id = '1001' # An identifier you define, that is unique to your Cloudflare account. Must be an integer.
# limit: the number of tokens allowed within a given period in a single Cloudflare location
# period: the duration of the period, in seconds. Must be either 10 or 60
# NOTE: distibuted over many servers
simple = { limit = 1, period = 60 }

[env.dev]
routes = [{ pattern = "dev.worker.me2christ.com", custom_domain = true }]
vars = { TURNSTILE_SECRET_KEY = '1x0000000000000000000000000000000AA' }
